Archived scripts and utilities

This directory contains files moved out of the main source tree because they are
either unused by the main flow, are helper scripts for one-off tasks, or are
older utilities kept for reference.

Files moved here:
- `deserter.py` — periodic member tracking script
- `copy_to_all_setka.py` — mass repost helper
- `reposter_setka.py` — reposting helper
- `pip_all_upgrade.py` — environment bootstrap / upgrade helper
- tests/tmp_*.py — temporary test/debug helpers

If you need to restore any archived file, copy it back into the repository root.
