  # Основное веб-приложение
  - type: web
    name: postopus-web
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: python -m uvicorn src.web.main_render:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
<<<<<<< HEAD
      - key: SECRET_KEY
        generateValue: true
      - key: VK_API_VERSION
        value: "5.131"
      - key: ENVIRONMENT
        value: "production"
      - key: DEBUG
        value: "false"

  # PostgreSQL Database
  - type: pserv
    name: postopus-db
    env: postgresql
    plan: starter
    databases:
      - name: postopus
        user: postopus_user

  # Redis for Celery/Caching
  - type: redis
    name: postopus-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru

  # Background Worker (Celery)
  - type: worker
    name: postopus-worker
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A src.tasks.celery_app worker --loglevel=info"
    envVars:
      - key: PYTHONPATH
        value: "/opt/render/project/src"
      - key: DATABASE_URL
        fromDatabase:
          name: postopus-db
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: LOG_LEVEL
        value: INFO

  # Celery Worker (как Web Service, так как Background Workers недоступны на бесплатном плане)
  - type: worker
    name: postopus-worker
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: cd src && celery -A tasks.celery_app worker --loglevel=info
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString

  # Celery Beat Scheduler (как Background Worker)
  - type: worker
    name: postopus-scheduler
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: cd src && celery -A tasks.celery_app beat --loglevel=info
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
<<<<<<< HEAD
      - key: ENVIRONMENT
        value: "production"
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString

  # Redis сервис
  - type: redis
    name: postopus-redis
    plan: free
    ipAllowList: []

databases:
  - name: mikrokredit-db
    plan: free
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
# render.yaml - Render.com Blueprint Configuration for Postopus
services:
  # Main Web Application
  - type: web
    name: postopus-web
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn src.web.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
    healthCheckPath: "/health"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
      - key: PYTHONPATH
        value: "/opt/render/project/src"
      - key: DATABASE_URL
        fromDatabase:
          name: postopus-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: VK_API_VERSION
        value: "5.131"
      - key: ENVIRONMENT
        value: "production"
      - key: DEBUG
        value: "false"
      - key: LOG_LEVEL
        value: "INFO"

  # Celery Worker
  - type: worker
    name: postopus-worker
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A src.tasks.celery_app worker --loglevel=info"
    envVars:
      - key: PYTHONPATH
        value: "/opt/render/project/src"
      - key: DATABASE_URL
        fromDatabase:
          name: postopus-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: ENVIRONMENT
        value: "production"

  # Redis Service
  - type: redis
    name: postopus-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

# PostgreSQL Database
databases:
  - name: postopus-db
    plan: free
=======
  # Основное веб-приложение
  - type: web
    name: postopus-web
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: python -m uvicorn src.web.main_render:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
<<<<<<< HEAD
      - key: SECRET_KEY
        generateValue: true
      - key: VK_API_VERSION
        value: "5.131"
      - key: ENVIRONMENT
        value: "production"
      - key: DEBUG
        value: "false"

  # PostgreSQL Database
  - type: pserv
    name: postopus-db
    env: postgresql
    plan: starter
    databases:
      - name: postopus
        user: postopus_user

  # Redis for Celery/Caching
  - type: redis
    name: postopus-redis
    plan: starter
    maxmemoryPolicy: allkeys-lru

  # Background Worker (Celery)
  - type: worker
    name: postopus-worker
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A src.tasks.celery_app worker --loglevel=info"
    envVars:
      - key: PYTHONPATH
        value: "/opt/render/project/src"
      - key: DATABASE_URL
        fromDatabase:
          name: postopus-db
          property: connectionString
=======
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: LOG_LEVEL
        value: INFO

  # Celery Worker (как Web Service, так как Background Workers недоступны на бесплатном плане)
  - type: worker
    name: postopus-worker
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: cd src && celery -A tasks.celery_app worker --loglevel=info
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString

  # Celery Beat Scheduler (как Background Worker)
  - type: worker
    name: postopus-scheduler
    env: python
    plan: free
    buildCommand: pip install -r requirements_render.txt
    startCommand: cd src && celery -A tasks.celery_app beat --loglevel=info
    envVars:
      - key: POSTGRES_HOST
        fromDatabase:
          name: mikrokredit-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: mikrokredit-db
          property: port
      - key: POSTGRES_DB
        fromDatabase:
          name: mikrokredit-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: mikrokredit-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: mikrokredit-db
          property: password
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
      - key: REDIS_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
<<<<<<< HEAD
      - key: ENVIRONMENT
        value: "production"
=======
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: postopus-redis
          property: connectionString

  # Redis сервис
  - type: redis
    name: postopus-redis
    plan: free
    ipAllowList: []

databases:
  - name: mikrokredit-db
    plan: free
>>>>>>> 0d4d3c6753eb3574a2a7cb8d6f485194f04b1f21
